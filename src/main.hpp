#ifndef MAIN_HPP
#define MAIN_HPP

int pin = 1;
int center_us = 1465;
int increment = 50; // us per step
int period = 20; // ms per step
int servo_angle_param = 255; // whatever the parameter is set to in the USB link
int lbound = 546;
int ubound = 2383;

#endif // MAIN_HPP